# Backend API base URL used by the frontend to call the payment API (development default).
VITE_API_URL=http://localhost:5000

# Public Razorpay Key ID (use the TEST MODE key while testing).
# This is a PUBLIC identifier — never put RAZORPAY_KEY_SECRET here.
VITE_RAZORPAY_KEY_ID=